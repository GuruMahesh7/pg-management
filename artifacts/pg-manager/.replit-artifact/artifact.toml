kind = "web"
previewPath = "/"
title = "PG Manager"
version = "1.0.0"
id = "artifacts/pg-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19144

[services.development]
run = "pnpm --filter @workspace/pg-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pg-manager", "run", "build" ]
publicDir = "artifacts/pg-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19144"
BASE_PATH = "/"
